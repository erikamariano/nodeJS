<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao Blog de NodeJS!</h1>
  <p class="lead">Construção de plataforma para aprender backend com NodeJS</p>
  <hr class="my-4">
  <p>Fazemos uso de tecnologias como Handlebars, Bootstrap, Express, MongoDB e muito mais!</p>

    {{#if user}} {{! se o usuário estiver cadastrado não vai aparecer o botão de criar a conta}}

    {{else}}

        <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma Conta</a>

    {{/if}}

  
</div>

<hr>

<h2>Posts Recentes</h2>


{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
         
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
            <hr>
            <small>Categoria: {{categoria.nome}}</small><br>
            <small>Data de Publicação: {{data}}</small>

        </div>
    </div>
{{else}}
    <h5>Nenhuma Postagem</h5>
{{/each}}